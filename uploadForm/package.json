{
  "name": "minnesinger.upload-form",
  "version": "0.0.0",
  "description": "Form page for uploading new tracks",
  "main": "src/index.tsx",
  "dependencies": {
    "id3-parser": "^3.0.0",
    "mime-db": "^1.52.0"
  },
  "scripts": {
    "build:dev": "../node_modules/.bin/webpack --mode development --config webpack.config.js",
    "serve": "../node_modules/.bin/webpack serve --mode development --config webpack.config.js",
    "build": "../node_modules/.bin/webpack --mode production --config webpack.config.js"
  }
}
